<%= simple_form_for [:admin, @page] do |f| %>
  <%= f.input :title %>
  <%= f.input :slug , :hint => "leave empty to generate the slug based on title", :required => false %>
  <%= f.input :body, :as => :text %>

  <%= f.button :submit %>
<% end %>

